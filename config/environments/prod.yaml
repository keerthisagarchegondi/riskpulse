# Production environment overrides
app:
  debug: false

monitoring:
  log_level: "WARNING"

database:
  pool_size: 30
  max_overflow: 20

kafka:
  consumer:
    batch_size: 500
    max_poll_records: 1000
  producer:
    acks: "all"
    retries: 5
